os:
  - linux
  - osx

language: cpp

compiler:
  - clang
  - gcc

script:
  - cmake .
  - make install
  - ./package.sh

deploy:
  provider: releases
  api-key: "GITHUB OAUTH TOKEN"
  file:  "dist/cppunit*.tgz"
  skip_cleanup: true
  on:
    tags: true

